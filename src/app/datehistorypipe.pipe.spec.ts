import { DatehistorypipePipe } from './datehistorypipe.pipe';

describe('DatehistorypipePipe', () => {
  it('create an instance', () => {
    const pipe = new DatehistorypipePipe();
    expect(pipe).toBeTruthy();
  });
});
